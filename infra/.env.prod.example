# NetGeo — variabel produksi (salin ke infra/.env.prod, JANGAN commit yang asli)
# Dipakai: docker compose -f infra/docker-compose.prod.yml --env-file infra/.env.prod up -d

# --- PostgreSQL ---
POSTGRES_USER=netgeo
POSTGRES_PASSWORD=ganti-dengan-password-kuat
POSTGRES_DB=netgeo

# --- Backend ---
SECRET_KEY=ganti-dengan-random-32+char
CORS_ORIGINS=https://netgeo.example.com
EMULATION_BACKEND=null            # null | containerlab | docker

# --- Image (registry) ---
BACKEND_IMAGE=ghcr.io/netgeo/backend:latest
FRONTEND_IMAGE=ghcr.io/netgeo/frontend:latest

# --- Networking ---
HTTP_PORT=80
